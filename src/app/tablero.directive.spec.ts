import { TableroDirective } from './tablero.directive';

describe('TableroDirective', () => {
  it('should create an instance', () => {
    const directive = new TableroDirective();
    expect(directive).toBeTruthy();
  });
});
